# Private — contributor bios / PII, kept local only, never committed
.claude/skills/team/
*.local.md
.dev.vars
# Local-only progress log maintained by the /loop watcher
hackathon-progress.md

# Secrets & env
.env
.env.local

# Dependencies & build output
node_modules/
dist/
build/
*.log
.DS_Store
.vercel/

# iOS / Xcode build artifacts
ios/**/build/
**/DerivedData/
**/xcuserdata/
*.xcuserstate

sample.webm

# eval harness data/artifacts
backend/eval-data/

# Toy demo target — lives in its own repo (github.com/Evgastap/linny)
toy-linear/
